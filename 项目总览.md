# 🎉 NovaTech 博客系统 - 完整项目总览

## 项目简介

这是一个现代化的全栈技术博客系统，包含：
- **前端**: React 19 + TypeScript + Tailwind CSS + Gemini AI
- **后端**: Java 17 + Spring Boot 3 + Spring Security + JWT + H2/MySQL

---

## 📁 项目结构

```
blog/
├── novatech-blog/              # React 前端项目
│   ├── components/             # UI 组件
│   ├── pages/                  # 页面组件
│   ├── services/               # API 服务
│   ├── data/                   # 本地数据
│   ├── package.json
│   └── vite.config.ts
│
└── novatech-backend/           # Spring Boot 后端项目
    ├── src/main/java/com/novatech/blog/
    │   ├── config/             # 配置类
    │   ├── controller/         # REST 控制器
    │   ├── dto/                # 数据传输对象
    │   ├── entity/             # JPA 实体
    │   ├── exception/          # 异常处理
    │   ├── repository/         # 数据访问
    │   ├── security/           # 安全配置
    │   └── service/            # 业务逻辑
    ├── src/main/resources/
    │   └── application.yml
    ├── pom.xml
    └── README.md
```

---

## 🔑 核心功能

### 前端功能
✅ 响应式设计 + 暗黑主题  
✅ 文章列表展示  
✅ 文章详情页面  
✅ Markdown 编辑器  
✅ 管理员登录  
✅ Gemini AI 内容生成（可选）  
✅ 流畅动画效果（Framer Motion）  

### 后端功能
✅ RESTful API  
✅ JWT 身份认证  
✅ 基于角色的访问控制（RBAC）  
✅ 文章 CRUD 操作  
✅ Slug 自动生成  
✅ 标签管理  
✅ CORS 跨域支持  
✅ 全局异常处理  
✅ H2 内存数据库（可切换 MySQL）  

---

## 🚀 快速启动

### 1️⃣ 启动后端

#### 使用 IntelliJ IDEA（推荐）
```
1. 打开 novatech-backend 文件夹
2. 等待 Maven 下载依赖
3. 运行 BlogApplication.java
4. 访问 http://localhost:8080
```

#### 使用命令行（需要 Maven）
```bash
cd novatech-backend
mvn spring-boot:run
```

**默认配置：**
- 端口: 8080
- 管理员账号: admin / admin123
- H2 控制台: http://localhost:8080/h2-console

### 2️⃣ 启动前端

```bash
cd novatech-blog
npm install
npm run dev
```

**访问：** http://localhost:5173

---

## 🔗 API 端点一览

### 认证模块
| 方法 | 端点 | 描述 | 权限 |
|------|------|------|------|
| POST | `/api/auth/login` | 登录获取 JWT | 公开 |
| POST | `/api/auth/logout` | 登出 | 公开 |

### 文章模块
| 方法 | 端点 | 描述 | 权限 |
|------|------|------|------|
| GET | `/api/posts` | 获取所有文章 | 公开 |
| GET | `/api/posts/{slug}` | 获取文章详情 | 公开 |
| POST | `/api/posts` | 创建文章 | 管理员 |
| PUT | `/api/posts/{id}` | 更新文章 | 管理员 |
| DELETE | `/api/posts/{id}` | 删除文章 | 管理员 |

---

## 📊 数据模型

### User（用户）
```java
{
  id: Long,
  username: String,
  password: String (BCrypt加密),
  role: Role (ADMIN/VISITOR)
}
```

### Post（文章）
```java
{
  id: Long,
  slug: String (URL友好的唯一标识),
  title: String,
  excerpt: String,
  content: String (HTML/Markdown),
  category: String,
  tags: List<String>,
  readTime: String,
  createdAt: LocalDateTime,
  updatedAt: LocalDateTime
}
```

---

## 🔧 前后端对接

### 前端需要修改的文件

#### 1. `services/blogService.ts`
```typescript
const API_BASE = 'http://localhost:8080/api';

// 获取所有文章
export const getAllPosts = async () => {
  const res = await fetch(`${API_BASE}/posts`);
  return res.json();
};

// 创建文章
export const savePost = async (post: PostRequest) => {
  const token = localStorage.getItem('novatech_auth_token');
  const res = await fetch(`${API_BASE}/posts`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
      'Authorization': `Bearer ${token}`
    },
    body: JSON.stringify(post)
  });
  return res.json();
};
```

#### 2. `services/authContext.tsx`
```typescript
const login = async (password: string) => {
  const res = await fetch('http://localhost:8080/api/auth/login', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({ username: 'admin', password })
  });
  
  if (res.ok) {
    const { token } = await res.json();
    localStorage.setItem('novatech_auth_token', token);
    setIsAdmin(true);
    return true;
  }
  return false;
};
```

---

## 🧪 测试流程

### 1. 测试登录
```bash
# PowerShell
$body = @{
    username = "admin"
    password = "admin123"
} | ConvertTo-Json

$response = Invoke-RestMethod -Uri "http://localhost:8080/api/auth/login" -Method Post -ContentType "application/json" -Body $body

$response
```

### 2. 测试获取文章列表
```bash
Invoke-RestMethod -Uri "http://localhost:8080/api/posts"
```

### 3. 测试创建文章
```bash
$token = "YOUR_JWT_TOKEN"
$headers = @{
    "Authorization" = "Bearer $token"
    "Content-Type" = "application/json"
}

$post = @{
    title = "测试文章"
    excerpt = "这是摘要"
    content = "<p>文章内容</p>"
    category = "Backend"
    tags = @("Java", "Spring")
    readTime = "5 min"
} | ConvertTo-Json

Invoke-RestMethod -Uri "http://localhost:8080/api/posts" -Method Post -Headers $headers -Body $post
```

---

## 🎯 技术栈详情

### 后端技术栈
- **Java**: 17
- **Spring Boot**: 3.2.0
- **Spring Security**: 6 (JWT认证)
- **Spring Data JPA**: 数据访问
- **H2 Database**: 内存数据库
- **Lombok**: 简化Java代码
- **jjwt**: JWT令牌处理
- **Maven**: 依赖管理

### 前端技术栈
- **React**: 19
- **TypeScript**: 类型安全
- **Vite**: 构建工具
- **Tailwind CSS**: 样式框架
- **Framer Motion**: 动画库
- **React Router**: 路由管理
- **Gemini AI**: 内容生成

---

## 📦 核心依赖

### 后端 (pom.xml)
```xml
- spring-boot-starter-web
- spring-boot-starter-data-jpa
- spring-boot-starter-security
- spring-boot-starter-validation
- h2database
- jjwt (0.12.3)
- lombok
```

### 前端 (package.json)
```json
- react: ^19.2.0
- react-router-dom: ^7.9.6
- framer-motion: ^12.23.24
- @google/genai: ^1.30.0
- lucide-react: ^0.555.0
```

---

## 🔐 安全机制

1. **JWT 认证**: 无状态的Token认证
2. **密码加密**: BCrypt加密存储
3. **CORS 配置**: 限制跨域访问来源
4. **角色权限**: @PreAuthorize注解控制
5. **异常处理**: 全局异常拦截器

---

## 🌟 项目亮点

✨ **现代化架构**: 前后端分离，RESTful设计  
✨ **安全可靠**: JWT + BCrypt + RBAC  
✨ **开箱即用**: 自动数据初始化  
✨ **易于扩展**: 清晰的分层架构  
✨ **灵活部署**: H2内存或MySQL持久化  
✨ **优雅设计**: Tailwind暗黑主题 + 流畅动画  

---

## 📖 文档导航

- **后端详细文档**: `novatech-backend/README.md`
- **快速启动指南**: `novatech-backend/快速启动指南.md`
- **API测试文档**: 见上方"测试流程"章节
- **前端项目文档**: `novatech-blog/README.md`

---

## 🛠️ 后续优化方向

- [ ] 文章评论功能
- [ ] 文章点赞收藏
- [ ] 用户注册系统
- [ ] 文章搜索功能
- [ ] 分页查询优化
- [ ] Redis缓存集成
- [ ] 图片上传功能
- [ ] Markdown编辑器增强
- [ ] 单元测试完善
- [ ] Docker容器化部署

---

## 📞 问题反馈

如果遇到问题：
1. 检查控制台日志
2. 查看 H2 数据库数据
3. 确认端口未被占用
4. 验证 JWT Token 格式

---

**🎊 祝您使用愉快！**
